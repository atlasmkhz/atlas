-- ═══════════════════════════════════════════════════════════════
-- ATLAS 광장 — 마이그레이션: 댓글 수정 정책 추가
-- (이미 supabase_schema.sql을 실행하셨다면, 이 파일만 추가로 실행하면 됩니다)
--
-- 기존 스키마에는 댓글 삭제(comments_delete_own) 정책만 있고
-- 수정 정책이 빠져 있었습니다. 댓글 수정 기능을 켜려면 필요합니다.
--
-- Supabase SQL Editor에 붙여넣고 Run 하세요.
-- ═══════════════════════════════════════════════════════════════

create policy "comments_update_own" on public.agora_comments
  for update using (auth.uid() = user_id);

-- ═══════════════════════════════════════════════════════════════
-- 완료. 이미 정책이 있다는 오류(policy already exists)가 뜨면
-- 이미 적용되어 있는 것이니 무시하셔도 됩니다.
-- ═══════════════════════════════════════════════════════════════
